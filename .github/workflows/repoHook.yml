name: Main Tubescope Workflow

on:
  repository_dispatch:
    types: [do-workflow]

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: isbang/compose-action@v1.5.1
        with:
          compose-file: "./compose.yml"